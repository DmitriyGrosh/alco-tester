import { reatomComponent } from "@reatom/react";

export const Home = reatomComponent(
    () => {
        return (
            <div>
                <h1>Home</h1>
            </div>
        );
    }
);